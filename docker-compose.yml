version: '2'

services:
  dev:
    build:
      context:  ./
      dockerfile: Dockerfile.dev
    volumes:
      - ${HOME}/.panoptes/:/root/.panoptes/
      - ${HOME}:${HOME}

  stable:
    build:
      context: ./
      dockerfile: Dockerfile.stable
    volumes:
      - ${HOME}/.panoptes/:/root/.panoptes/
      - ${HOME}:${HOME}
